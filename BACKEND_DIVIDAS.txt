================================================================================
DÍVIDAS DO BACKEND — Apex Keys
(itens a implementar no servidor/API)
================================================================================

REF: apex-keys-api (pasta no repo) — análise em 2025-03-23

[IMPLEMENTADO na apex-keys-api]
  1. FOTO DE PERFIL (avatar_url) — POST /users/me/avatar, modelo User.avatar_url
  2. SIGNUP — POST /auth/signup com full_name, email, password, whatsapp, pix_key
  3. CHAVE PIX — coluna pix_key no User, retorno em UserPublic
  4. EDIÇÃO DE PERFIL — PATCH /users/me com full_name, whatsapp, pix_key

[PENDENTE / FUTURO]
  5. Recuperação de senha
  6. Verificação de email
  7. (Opcional) Rifas com summary, genres, series no DB — frontend envia, API ignora por ora

[ROTAS ADMIN]
  - API expõe /api/v1/admin/* e alias /admin/* (compatibilidade)
  - Frontend deve usar /api/v1/admin/*; se Vercel mostrar 404 em /admin/raffles,
    fazer redeploy do site (código já corrigido) ou usar o alias na API

================================================================================